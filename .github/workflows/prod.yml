on:
  push:
    branches: [develop, devops/test-gh-actions]

name: Deploy Petinder-Back

env:
  IMAGE: ${{ secrets.DOCKER_USERNAME }}/petinder:back

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK - Java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '21'

      - name: Build do Project
        run: mvn -B clean package -DskipTests

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ env.IMAGE }}

  deploy:
    needs: build
    strategy:
      matrix:
        runner: ["pt-01", "pt-02"]
    runs-on: [self-hosted, ${{ matrix.runner }}]
    env:
      WIPE_RABBITMQ: "true"
    steps:
      - name: Info do runner
        run: |
          echo "Runner target: ${{ matrix.runner }}"
          hostname
          whoami

      - name: Liberar porta 8080 e 3306 no firewall (ufw) — só se aplicável
        run: sudo ufw allow 8080/tcp && sudo ufw allow 3306/tcp || true

      - name: Checkout repository (required to access compose file)
        uses: actions/checkout@v3

      - name: Ensure docker compose and start RabbitMQ (if not running)
        run: |
          echo "Removing any existing rabbitmq containers by name and ancestor image..."
          sudo docker ps -a --filter "name=rabbitmq-petinder" --format '{{.ID}}' | xargs -r sudo docker rm -f || true
          sudo docker ps -a --filter "ancestor=rabbitmq" --format '{{.ID}}' | xargs -r sudo docker rm -f || true

          echo "Tearing down any existing compose stack..."
          if sudo docker compose version >/dev/null 2>&1; then
            if [ "$WIPE_RABBITMQ" = "true" ]; then
              sudo docker compose -f compose.yaml down --volumes --remove-orphans || true
            else
              sudo docker compose -f compose.yaml down --remove-orphans || true
            fi
          elif command -v docker-compose >/dev/null 2>&1; then
            if [ "$WIPE_RABBITMQ" = "true" ]; then
              sudo docker-compose -f compose.yaml down --volumes --remove-orphans || true
            else
              sudo docker-compose -f compose.yaml down --remove-orphans || true
            fi
          else
            echo "docker compose not found on runner; exit" >&2
            exit 1
          fi

          if [ "$WIPE_RABBITMQ" = "true" ]; then
            sudo docker volume rm rabbitmq-data || true
          fi

          echo "Starting rabbitmq via docker compose (pull + force recreate)"
          if sudo docker compose version >/dev/null 2>&1; then
            sudo docker compose -f compose.yaml pull || true
            sudo docker compose -f compose.yaml up -d --force-recreate --remove-orphans
          elif command -v docker-compose >/dev/null 2>&1; then
            sudo docker-compose -f compose.yaml pull || true
            sudo docker-compose -f compose.yaml up -d --force-recreate --remove-orphans
          fi

      - name: Wait for RabbitMQ healthcheck to be healthy
        run: |
          CONTAINER_NAME=rabbitmq-petinder
          MAX_SECONDS=120
          SLEEP_INTERVAL=5
          elapsed=0
          while [ $elapsed -lt $MAX_SECONDS ]; do
            status=$(sudo docker inspect --format '{{.State.Health.Status}}' ${CONTAINER_NAME} 2>/dev/null || echo "none")
            echo "Current health status: ${status}"
            if [ "${status}" = "healthy" ]; then
              echo "${CONTAINER_NAME} is healthy"
              exit 0
            fi
            sleep ${SLEEP_INTERVAL}
            elapsed=$((elapsed + SLEEP_INTERVAL))
          done
          echo "${CONTAINER_NAME} did not become healthy within ${MAX_SECONDS}s"
          sudo docker logs --tail 200 ${CONTAINER_NAME} || true
          exit 1

      - name: Login no Docker Hub no host de deploy
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | sudo docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Pull image do Docker Hub
        run: sudo docker pull "${{ env.IMAGE }}"

      - name: Stop e remove container existente (caso haja)
        run: |
          TARGET_NAME="deploy-back"
          TARGET_IMAGE="${{ env.IMAGE }}"
          sudo docker ps -a --filter "name=$TARGET_NAME" --format '{{.ID}}' | xargs -r sudo docker rm -f || true
          sudo docker ps -a --filter "ancestor=$TARGET_IMAGE" --format '{{.ID}}' | xargs -r sudo docker rm -f || true
          sudo docker images --format '{{.Repository}}:{{.Tag}} {{.ID}}' | awk -v img="$TARGET_IMAGE" '$0 ~ img {print $2}' | xargs -r sudo docker rmi -f || true

      - name: Criar env file seguro e rodar container usando --env-file
        run: |
          ENV_FILE="$RUNNER_TEMP/petinder.env"
          mkdir -p "$RUNNER_TEMP"
          cat > "$ENV_FILE" <<'EOF'
          SPRING_PROFILES_ACTIVE=prod
          AWS_ACCESS_KEY_ID=${{ secrets.SEU_ACCESS_ID }}
          AWS_SECRET_ACCESS_KEY=${{ secrets.SEU_SECRET_KEY }}
          AWS_SESSION_TOKEN=${{ secrets.SEU_SESSION_TOKEN }}
          SPRING_RABBITMQ_HOST=host.docker.internal
          SPRING_RABBITMQ_PORT=5672
          SPRING_RABBITMQ_USERNAME=admin
          SPRING_RABBITMQ_PASSWORD=admin
          SENHA_PETINDER_EMAIL=${{ secrets.SENHA_PETINDER_EMAIL }}
          EOF
          sudo sed -i 's/\r$//' "$ENV_FILE" || true
          sudo chmod 600 "$ENV_FILE"

          NETWORK_NAME=$(sudo docker inspect --format '{{range $k,$v := .NetworkSettings.Networks}}{{printf "%s\n" $k}}{{end}}' rabbitmq-petinder | head -n1 || true)
          if [ -n "$NETWORK_NAME" ]; then
            NETWORK_ARG="--network $NETWORK_NAME"
            RABBIT_HOST="rabbitmq-petinder"
          else
            NETWORK_ARG=""
            RABBIT_HOST="host.docker.internal"
          fi

          sudo docker run -d --name deploy-back \
            $NETWORK_ARG \
            --add-host=host.docker.internal:host-gateway \
            -e SPRING_RABBITMQ_HOST=$RABBIT_HOST \
            --restart unless-stopped \
            --env-file "$ENV_FILE" \
            -p 8080:8080 \
            "${{ env.IMAGE }}"

          if command -v shred >/dev/null 2>&1; then
            sudo shred -u "$ENV_FILE" || sudo rm -f "$ENV_FILE"
          else
            sudo rm -f "$ENV_FILE"
          fi

      - name: Show last container logs (sanity)
        run: sudo docker logs --tail 200 deploy-back || true